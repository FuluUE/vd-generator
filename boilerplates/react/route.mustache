import { connect } from 'dva';
import {{name}} from '{{{relativePath}}}/components/{{{groupPath}}}{{name}}';


const mapStateToProps = (state) => {
  return {
    ...state,
  };
}

export default connect(mapStateToProps)({{name}});
