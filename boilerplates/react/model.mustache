// import queryString from 'query-string';
import * as {{camelCaseName}}Service from '../services/{{camelCaseName}}';

export default {
  namespace: '{{camelCaseName}}',
  state: {},
  subscriptions: {
    setup({history, dispatch}, onError) {
      // return history.listen( ({pathname, search}) => {
      //   const query = queryString.parse(search);
      //   if (pathname === '/') {
      //
      //   }
      // });
    }
  },
  effects: {
    *tests(action, {call, put}) {
      const testRes = yield call({{camelCaseName}}Service.{{name}});
      yield put({
        type: 'test',
        payload: {
          data: testRes
        },
      });
    },
  },
  reducers: {
    test(state, {payload}) {
      return {
        ...state,
        ...payload,
      };
    }
  },
};
