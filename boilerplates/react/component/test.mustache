import React from 'react';
import { shallow } from 'enzyme';
import {{name}} from '../';


describe('Main', () => {
  it('should normal shallow', () => {
    const wrapper = shallow(<{{name}} />);
  });
});
